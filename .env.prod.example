# Copy to .env.prod on the VPS (never commit .env.prod) and fill in real values.
# Used by: docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build

POSTGRES_DB=lenormand
POSTGRES_USER=lenormand
POSTGRES_PASSWORD=change-me-to-a-real-random-password
